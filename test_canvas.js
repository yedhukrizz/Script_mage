// simple conceptual code
const line = "Hello world this is a test";
const words = line.split(/(\s+)/);
let currentX = 0;
for (const word of words) {
   // const w = measureText(word).width;
   // fillText(word, currentX, y);
   // currentX += w;
}
